using System;
using System.IO;
using System.Text;

public class Writer {

    public Writer() {

    }
    //if (type.IsPrimitive || type == typeof(string)) {
        //WritePrimitive();
    //        body.Append("F|P fieldName");
    //        body.Append(type.Name);
    //        body.Append(ValueSymbol);
    //        body.Append(value.ToString());

    //    }
    //    else if (type.IsEnum) {

    //    }
    //    else if (type.IsValueType) {

    //    }
    //    else if (type.IsClass) {

    //    }
    //    else {
    //        throw new Exception("Unknown type : " + type.Name);
    //    }

}