using UnityEngine;

public class EntityAbilityDatabase : ScriptableObject {
    //todo maybe just use a json file since we really just want pointers into the main AbilityDatabases
    //need to track or rebuild modifiers also, probably keep a seperate serialized list of those with pointers to the abilities they apply to
}